cd client
npm install
cd ..

Inside client:
        npm install axios react-router-dom
         Optional: ( npm install @reduxjs/toolkit react-redux
          npm install tailwindcss @tailwindcss/vite )

         npm install tailwindcss @tailwindcss/vite

Start Frontend:
            cd client
            npm run dev